# ⚡ Быстрый старт - Lissa Furniture

## 🏠 Локальная разработка

```bash
# 1. Установите зависимости (первый раз)
npm install

# 2. Запустите сервер
npm start
# или двойной клик на start.bat (Windows)

# 3. Откройте в браузере
http://localhost:3000

# 4. Админка
http://localhost:3000/admin
Логин: admin
Пароль: admin123
```

---

## 🚀 Деплой в продакшн

### Вариант 1: Простой (один хостинг)
Используйте Railway/Render с Node.js - весь проект работает из коробки.

### Вариант 2: Разделенный (рекомендуется)
- **Frontend** → Netlify (папка `public/`)
- **Backend** → Railway (весь проект)

📖 **Подробная инструкция:** `DEPLOY_GUIDE.md`

---

## 📁 Структура

```
lissa-furniture/
├── admin/           # Админка (также в public/admin/)
├── public/          # Frontend (деплоить на Netlify)
│   ├── admin/       # Админка UI
│   ├── css/         # Стили
│   ├── js/          # JavaScript
│   ├── images/      # Картинки сайта
│   ├── uploads/     # Загруженные фото
│   └── index.html   # Главная страница
├── server/          # Backend API
│   └── server.js    # Express сервер
├── lissa.db        # База данных
└── package.json    # Зависимости
```

---

## 🔧 Настройка API

После деплоя Backend на Railway:

1. Откройте `public/js/config.js`
2. Замените строку 13:
```javascript
production: 'https://YOUR-RAILWAY-URL.up.railway.app'
```
3. Сохраните и задеплойте Frontend на Netlify

---

## ✅ Checklist перед деплоем

- [ ] Сменили пароль админки в `server/server.js`
- [ ] Обновили Railway URL в `config.js`
- [ ] Проверили что база данных `lissa.db` существует
- [ ] Загрузили фото в `public/uploads/`

---

## 🆘 Проблемы?

**Сервер не запускается:**
```bash
rm -rf node_modules
npm install
npm start
```

**База данных не работает:**
- Убедитесь что файл `lissa.db` существует
- Проверьте права доступа к файлу

**Фото не загружаются:**
- Проверьте папку `public/uploads/`
- Убедитесь что у сервера есть права на запись

---

**📞 Контакты проекта:**
- Telegram: @lissa_furniture
- WhatsApp: +382 67 584 423
