# 🚀 Инструкция по деплою Lissa Furniture

Проект разделен на **Frontend** (Netlify) и **Backend** (Railway) для оптимальной работы.

---

## 📦 Часть 1: Деплой Backend на Railway

Backend включает API, базу данных и админку.

### Шаг 1: Создайте аккаунт на Railway

1. Откройте https://railway.app
2. Нажмите **"Start a New Project"**
3. Войдите через GitHub

### Шаг 2: Создайте новый проект

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Если репозитория нет - выберите **"Empty Project"**

### Шаг 3: Загрузите Backend

**Вариант A: Через GitHub (рекомендуется)**
1. Загрузите весь проект на GitHub
2. В Railway выберите свой репозиторий
3. Railway автоматически определит Node.js проект

**Вариант B: Через Railway CLI**
```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите в аккаунт
railway login

# Инициализируйте проект (в корне lissa-furniture/)
railway init

# Деплой
railway up
```

### Шаг 4: Настройте переменные окружения

В Railway Dashboard:
1. Откройте ваш проект
2. Перейдите в **Variables**
3. Добавьте:
   ```
   PORT=3000
   NODE_ENV=production
   ```

### Шаг 5: Получите URL вашего Backend

После деплоя Railway даст вам URL типа:
```
https://lissa-furniture-production.up.railway.app
```

**⚠️ ВАЖНО:** Скопируйте этот URL!

---

## 🌐 Часть 2: Деплой Frontend на Netlify

Frontend - это статический сайт из папки `public/`.

### Шаг 1: Подготовьте Frontend

1. Откройте файл `public/js/config.js`
2. Замените URL в строке 13:
   ```javascript
   production: 'https://ВАШ-RAILWAY-URL.up.railway.app'
   ```
   Вставьте URL из Railway (Часть 1, Шаг 5)

3. Сохраните файл

### Шаг 2: Создайте аккаунт на Netlify

1. Откройте https://netlify.com
2. Нажмите **"Sign up"**
3. Войдите через GitHub

### Шаг 3: Деплой на Netlify

**Вариант A: Drag & Drop (самый простой)**
1. В Netlify Dashboard нажмите **"Add new site"** → **"Deploy manually"**
2. Перетащите папку `public/` в окно браузера
3. Готово! Netlify даст вам URL типа `lissa-furniture.netlify.app`

**Вариант B: Через Git**
1. Загрузите проект на GitHub
2. В Netlify: **"Add new site"** → **"Import from Git"**
3. Выберите репозиторий
4. Настройки:
   - **Build command:** (оставить пустым)
   - **Publish directory:** `public`
5. Нажмите **"Deploy"**

### Шаг 4: Настройте домен (опционально)

1. В Netlify Dashboard откройте **"Domain settings"**
2. Нажмите **"Add custom domain"**
3. Введите ваш домен (например, `lissa-furniture.me`)
4. Следуйте инструкциям Netlify для настройки DNS

---

## ✅ Проверка работы

### Проверьте Frontend
1. Откройте ваш Netlify URL
2. Сайт должен загрузиться
3. Проверьте переключение языков
4. Должны отображаться проекты из портфолио

### Проверьте Backend
1. Откройте `ваш-netlify-url.netlify.app/admin`
2. Войдите: `admin` / `admin123`
3. Попробуйте загрузить фото
4. Если фото загружается - всё работает!

### Если что-то не работает:

**Ошибка: "Failed to fetch portfolio"**
- Проверьте что в `config.js` правильный Railway URL
- Проверьте что Railway проект запущен (не "спит")

**Ошибка: "CORS error"**
- В Railway откройте логи
- Проверьте что ваш Netlify домен добавлен в CORS
- Backend автоматически разрешает все `.netlify.app` домены

**Админка не загружает фото**
- Проверьте Railway логи
- Убедитесь что папка `uploads/` создана на Railway
- Railway может требовать использование S3/Cloudinary для файлов

---

## 🔐 Безопасность

### ⚠️ ОБЯЗАТЕЛЬНО после деплоя:

1. **Смените пароль админки**
   - Откройте `server/server.js`
   - Найдите строки ~85-86:
   ```javascript
   const ADMIN_USERNAME = 'admin';
   const ADMIN_PASSWORD = 'admin123';  // ← СМЕНИТЕ!
   ```
   - Измените на свой пароль
   - Сделайте коммит и redeploy на Railway

2. **Ограничьте доступ к админке**
   - Рекомендуется добавить IP whitelist
   - Или использовать VPN для доступа к админке

---

## 💾 Backup базы данных

База данных `lissa.db` хранится на Railway. Для бэкапа:

1. В Railway CLI:
   ```bash
   railway run bash
   cat lissa.db > backup.db
   ```

2. Или настройте автоматический бэкап в Railway Volumes

---

## 📝 Полезные команды

### Railway CLI
```bash
# Просмотр логов
railway logs

# Подключиться к проекту
railway link

# Открыть проект в браузере
railway open

# Перезапуск
railway up
```

### Netlify CLI
```bash
# Установка
npm install -g netlify-cli

# Деплой из папки public
cd public && netlify deploy --prod

# Просмотр логов
netlify logs
```

---

## 🆘 Поддержка

**Если возникли проблемы:**

1. Проверьте логи:
   - Railway: `railway logs`
   - Netlify: вкладка "Deploys" → "Deploy log"

2. Частые проблемы:
   - **"Module not found"** - запустите `npm install` на Railway
   - **"Port already in use"** - Railway автоматически использует $PORT
   - **"Database locked"** - перезапустите Railway проект

---

## 🎯 Итоговая структура

```
Frontend (Netlify)           Backend (Railway)
├── index.html      ------>  ├── server/server.js
├── css/                     ├── lissa.db (база)
├── js/                      ├── uploads/ (фото)
├── images/                  └── admin/ (тоже на Railway)
└── admin/ (UI)
    └── API calls -------->  └── /api/* (endpoints)
```

---

**✨ Готово! Теперь у вас:**
- ✅ Быстрый фронтенд на Netlify (бесплатно)
- ✅ Надежный бэкенд на Railway (бесплатно до 500 часов/месяц)
- ✅ Работающая админка для управления портфолио
- ✅ Автоматическое масштабирование

**🚀 Сайт готов к продакшену!**
